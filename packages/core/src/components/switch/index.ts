export interface SwitchConfig {
  /**
   * Whether the switch is checked
   * @default false
   */
  checked?: boolean;
  
  /**
   * Whether the switch is disabled
   * @default false
   */
  disabled?: boolean;
}
